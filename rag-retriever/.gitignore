# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/

# Virtualenv
.venv/
venv/

# Vector store data / lancedb
*.lance/
.rag/
.rag-retriever/

# Vendored offline assets (embedding ONNX ~90MB + tiktoken BPE), fetched at
# release-build time by scripts/fetch_bundled_model.py and force-included into the
# wheel via pyproject artifacts. Kept out of git to avoid bloating history.
rag_retriever/_models/
rag_retriever/_tiktoken/

# OS
.DS_Store
Thumbs.db
