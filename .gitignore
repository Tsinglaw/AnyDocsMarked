# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage

# Virtualenv / uv
.venv/
venv/
# uv.lock is committed (not ignored): it pins exact dependency versions so CI
# and `uv run`/`uv sync` builds are reproducible for these distributable tools.

# 构建产物 / 向量库 / 案件数据（隐私）
dist/
*.lance/
.rag/
原始资料/
_md/
案件/
*案件*/
_smoke/
_interop/

# OS
.DS_Store
Thumbs.db
